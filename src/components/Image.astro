---
const { src, alt, height, width } = Astro.props;
---

<style is:global define:vars={{ height, width }}>
  .image {
    min-height: var(--height);
    width: var(--width);
  }
</style>

<img src={src} alt={alt} />
